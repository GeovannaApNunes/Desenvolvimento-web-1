function calc(){
            soma.value = parseInt(v1.value) + parseInt(v2.value);
            sub.value = parseInt(v1.value) - parseInt(v2.value);
            multi.value = parseInt(v1.value) * parseInt(v2.value);
            divi.value = parseInt(v1.value) / parseInt(v2.value);
           
        }

